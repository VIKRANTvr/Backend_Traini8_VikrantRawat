package com.traini8;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication // Starting point for Spring Boot application
public class Traini8Application {
    // The main method is the entry point for the Spring Boot application

    public static void main(String[] args) {
        // This line runs the Spring Boot application, which starts the server

        SpringApplication.run(Traini8Application.class, args);
    }
}
